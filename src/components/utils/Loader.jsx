import React from 'react';
import { CircularLoader } from '@dhis2/ui-core';
import { CenterLoader } from '../Styles';

export const Loader = () => (
    <CenterLoader>
        <CircularLoader />
    </CenterLoader>
)

